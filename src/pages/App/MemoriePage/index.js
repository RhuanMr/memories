import React from 'react';
import MemoriesComponent from '../../../components/MemorieComponent';

const Memorie = ({route,navigation}) => <MemoriesComponent route={route}  navigation={navigation} />  ;

export default Memorie;
